# XDG Base Directories
# Setting variables that reuse your $HOME
# XDG_CACHE_HOME   DEFAULT=@{HOME}/.cache
# XDG_CONFIG_HOME  DEFAULT=@{HOME}/.config
# XDG_DATA_HOME    DEFAULT=@{HOME}/.local/share

# You can reuse XDG_RUNTIME_DIR for runtime files
# ICEAUTHORITY     DEFAULT=${XDG_RUNTIME_DIR}/ICEauthority

# Tools
# You can define variables as VARIABLE=VALUE pair
# PAGER            DEFAULT=less
# BROWSER          DEFAULT=firefox
DIFFPROG         DEFAULT=meld

# SSH
# SSH_KEY_PATH     DEFAULT=@{HOME}/.ssh/id_rsa
SSH_AUTH_SOCK DEFAULT="${XDG_RUNTIME_DIR}/ssh-agent.socket"

# Temas GTK-QT
# QT_QPA_PLATFORMTHEME    DEFAULT=qt5ct
# QT_AUTO_SCREEN_SCALE_FACTOR	DEFAULT=0
# GTK2_RC_FILES						DEFAULT="@{HOME}/.gtkrc-2.0"

# GnuPG
# You can reuse variables you already defined
# GNUPGHOME        DEFAULT=${XDG_CONFIG_HOME}/gnupg

# Incorrect: you can't reuse other variables in VARIABLE=VALUE pair
# GNUPGHOME=${XDG_CONFIG_HOME}/gnupg

#Incorrect: missing {}
# GNUPGHOME        DEFAULT=$XDG_CONFIG_HOME/gnupg

# # Locale
# LANG            DEFAULT=pt_BR.UTF-8
# LANGUAGE				DEFAULT=${LANG}
# LC_CTYPE				DEFAULT=${LANG}
# LC_NUMERIC			DEFAULT=${LANG}
# LC_TIME					DEFAULT=${LANG}
# LC_COLLATE			DEFAULT=C
# LC_MONETARY			DEFAULT=${LANG}
# LC_MESSAGES			DEFAULT=${LANG}
# LC_PAPER				DEFAULT=${LANG}
# LC_NAME					DEFAULT=${LANG}
# LC_ADDRESS			DEFAULT=${LANG}
# LC_TELEPHONE		DEFAULT=${LANG}
# LC_MEASUREMENT	DEFAULT=${LANG}
# LC_IDENTIFICATION	DEFAULT=${LANG}
# LC_ALL          DEFAULT=${LANG}

# Configurando caminhos
# PATH           DEFAULT=${PATH}:/usr/local/texlive/2020/bin/x86_64-linux
# MANPATH        DEFAULT=${MANPATH}:/usr/local/texlive/2020/texmf-dist/doc/man
# INFOPATH       DEFAULT=${INFOPATH}:/usr/local/texlive/2020/texmf-dist/doc/info
